<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="user">
	<insert id="insert" parameterType="uservo">
		<![CDATA[insert into user values(null, #{name}, #{email}, #{password}, #{gender}, now())]]> <!-- 태그가 아님을 알려줌 (데이터) -->
	</insert>
	
	<select id="getByEmailAndPassword" parameterType="map" resultType="uservo"> <!-- map, string, integer 내장 -->
		<![CDATA[select no, name from user where email = #{email} and password = #{password}]]>
	</select>
	
	<select id="getByEmail" parameterType="string" resultType="uservo">
		<![CDATA[select no, name from user where email = #{email}]]> <!-- 기본 타입의 값이 하나 넘어올 경우 #{}으로 아무거나 적어주면 됨 -->
	</select>
	
	<select id="getByNo" parameterType="long" resultType="uservo">
		<![CDATA[select no, name, email, gender from user where no = #{no}]]>
	</select>
	
	<update id="update" parameterType="uservo">
		<choose>
			<when test="!password.equals('') and !''.equals(name)">
				<![CDATA[update user set name = #{name}, password = #{password}, gender = #{gender} where no = #{no}]]>
			</when>
			<when test="password.equals('') and !''.equals(name)">
				<![CDATA[update user set name = #{name}, gender = #{gender} where no = #{no}]]>
			</when>
		</choose>
	</update>
</mapper>
